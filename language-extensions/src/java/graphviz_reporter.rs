use suzaku_extension_sdk::language::reporter::Reporter;



pub struct GraphvizReporter;

impl Reporter for GraphvizReporter {
    fn new() -> Self {
        GraphvizReporter {}
    }

    fn generate() {
        
    }
}